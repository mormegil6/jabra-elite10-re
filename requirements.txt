# Jabra Elite 10 Gen 2 head tracker OSC bridge dependencies (macOS / cross-platform)
bleak>=0.21        # BLE via CoreBluetooth (macOS) / BlueZ (Linux) / WinRT (Windows)
python-osc>=1.8    # OSC UDP output
